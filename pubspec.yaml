name: drag_list
description: Flutter list widget that allows to drag and drop items and define custom drag handle widget.
version: 1.0.8
homepage: https://github.com/tomwyr/drag-list/

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  async: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
